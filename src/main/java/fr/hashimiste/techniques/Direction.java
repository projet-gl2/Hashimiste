package fr.hashimiste.techniques;

/**
* Enumerate contenant les directions cardinales pour se repérer dans la grille de jeu
*/
public enum Direction {
    /**
     * Indique la direction vers le haut.
     */
    NORD,
    /**
     * Indique la direction vers la droite.
     */
    EST,
    /**
     * Indique la direction vers le bas.
     */
    SUD,
    /**
     * Indique la direction vers la gauche.
     */
    OUEST;
}
